# ProxNest Landing Page

Static site built with React + Vite + Tailwind CSS. Deployed to Cloudflare Pages.

## Dev

```bash
npm install
npm run dev
```

## Deploy

```bash
npm run build
npx wrangler pages deploy dist --project-name=proxnest
```
